[workspace]

members = [
    "enclave",
    "parent",
    "shared",
]

[workspace.dependencies]
em-app = { version = "0.5.1" }
em-client = { version = "4.0.1" }
attestation-client = { git = "https://github.com/fortanix/attestation.git", package = "client", branch = "dlegalov/RTE-1101-integrate-attestation-repo" }
