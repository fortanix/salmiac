# Converter API Model 

* The container converter consumes a request JSON file via the `--request-file` argument and produces a JSON response.

* The complete request and response model spec is available under `docs/models.openapi.yaml`

* Sample request and response JSON files are available under `examples` directory.
